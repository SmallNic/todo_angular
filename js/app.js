(function(){

  //The first argument is the name of the angular module
  //The second argument is an array of modules on which the current module will depend on.
  //We immediately invoke in order to start the angular application and to prevent the pollution of the global namespaces
  angular.module("todo", [])
})()
